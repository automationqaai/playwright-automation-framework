export type UserData = {
  username: string;
  password: string;
};

export type ProductData = {
  name: string;
  price: string;
};

export type CustomerData = {
  firstName: string;
  lastName: string;
  postalCode: string;
};
